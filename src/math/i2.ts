/**
 * 函数“add”将两个数字作为参数并将它们的总和记录到控制台。
 * 
 * :param n: number - 该参数表示要添加的第一个数字。
 * :type n: number
 * :param b: 参数“b”是一个数字，将添加到函数中的参数“n”。
 * :type b: number
 */
function add(n: number, b: number) {
    console.log(n + b);
}
export { add };
